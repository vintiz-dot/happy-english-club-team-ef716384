# AI tool directories
.claude/
.lovable/

# Nested Lovable working clone — a separate repo that lives inside this
# folder for pushing to Lovable. It must NOT be tracked here (it was
# accidentally committed as a gitlink, which broke CI's submodule clone).
new/

# ── Secrets — NEVER commit these ─────────────────────────────────────
# Google Cloud service-account keys (Vision OCR + Custom Search)
GOOGLE_CREDENTIALS.json
customimage.json
google-creds*.json
*service-account*.json
*-credentials.json
gcp-*.json

# Local env overrides & token files (the committed .env holds ONLY
# publishable client values — server secrets go in Supabase secrets)
.env.local
.env.*.local
.env.secrets
*.token
token.json
credentials.json

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.loveable
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.bun-cache
